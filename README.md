Simple File Server on Nodejs basic libs

For Setup project:

``npm install``

Running project:

Developer mode:

``npm run start:dev``

Production mode:

``npm run start``

Upload file:

`PUT /files/:filename`

Get File:

`GET /files/:filename`
